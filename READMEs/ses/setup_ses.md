# STEPS

## Configuration sets

- Create Configuration set

# Create Identity

- Created an identity

# Add Route53 records

- Add DKIM records
- and MAIL FROM records

## Reference the screenshots

in `screenshots_of_setup_process`

## needed iam role to have `ses:SendTemplatedEmail` perms

User `arn:aws:iam::333427308013:user/<YOUR_USER_NAME>' is not authorized to perform `ses:SendTemplatedEmail' on resource `arn:aws:ses:us-east-1:333427308013:identity/<YOUR_VERIFIED_SES_IDENTITY>'
